TODO LIST

Major:

   * hypergraphs
   * hyperarchical graphs support
   * list-string storage optimization (values replaced by corresponding idx)
   * memory optimization (strings replaced by their real type like int) ; create a wrapper to access data?

Minor:
   * Logger class for info(), warn() and err() messages
